import { StyleSheet } from "react-native";
import R from "res/R";

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        marginHorizontal: 32,
        marginTop: 32,
        marginBottom: 16,
        zIndex: 999,
    },
    text: {
        fontSize: 26,
    },
    appVersion: {
        marginTop: 32,
    },
});
