import { StyleSheet, View } from "react-native";
import R from "res/R";

export default StyleSheet.create({
    flex: {
        flex: 1,
    },
    descriptionContainer: {
        margin: R.dimensions.element.padding.normal,
    },
    space: {
        marginTop: R.dimensions.element.space.small,
    },
});
