import { StyleSheet } from "react-native";
import R from "res/R";

export default StyleSheet.create({
    container: {},
    playerContainer: {
        position: "absolute",
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    },
});
