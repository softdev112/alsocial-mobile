import { StyleSheet } from "react-native";

export default StyleSheet.create({
    reaction: {
        alignItems: "center",
        justifyContent: "center",
        height: "100%",
        flexGrow: 1,
    },
});
