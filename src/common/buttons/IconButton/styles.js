import { StyleSheet } from "react-native";
import R from "res/R";

export default StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: R.colors.transparent,
        height: R.dimensions.button.reaction.normal,
        width: R.dimensions.button.reaction.normal,
    },
});
