import { StyleSheet } from "react-native";
import R from "res/R";

export default StyleSheet.create({
    container: {
        paddingLeft: R.dimensions.element.padding.normal,
        paddingRight: R.dimensions.element.padding.normal,
    },
});
